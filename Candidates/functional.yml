# we are specifying some domain and hoping that it will be somehow better (than whithout). How?
   purpose_of_tile:
     # could only be sole
      type: purpose_of_tile
      props:
        domain: string # better specification of what domain?
          thing: string
        actions: [action] # what will people do better using this tile
        # use wrapped _createEvent or _giveEvidence action to specify what event you want to see here
        comment: string # and add some free text here if needed

  # what playing this tile will give to supertile

    NameTileGives:
    # could be multiple
      type: tile_gives
      props:
        title: string
        to_whom: ROLE_NAME | TILE_ID
        benefit: string
